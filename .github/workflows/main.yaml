name: Hello World Workflow

on: 
  push: 
    branches:
      - main
  pull_request:
    branches:
      - main
  

jobs:
  hello_world:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello, World!"
